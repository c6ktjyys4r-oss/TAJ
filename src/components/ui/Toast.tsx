/**
 * Toast — thin re-export of sonner so every consumer imports from one place.
 *
 *   import { toast }   from '../ui/Toast';   // programmatic API
 *   import { Toaster } from '../ui/Toast';   // mount once in the app root
 */
export { toast, Toaster } from 'sonner';
